"""Models, migrations, seed.

See `migrate.py` (migration runner), `migrations/` (plain SQL, applied in order),
`connection.py` (runtime app_user connections, org-scoped) and `repository.py` (the
org-scoped repository helper every tenant-table query must go through).
"""
