# Base URL of the backend API (see backend/README.md). Defaults to the local dev
# server if unset — copy this file to .env.local and adjust for a deployed backend.
VITE_API_BASE_URL=http://localhost:8000
